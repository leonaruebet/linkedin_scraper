{
  "name": "apollo-linkedin-scraper",
  "version": "1.0.0",
  "description": "Apollo-like LinkedIn data extraction system",
  "scripts": {
    "scrape": "ts-node --project tsconfig.node.json scripts/scrape-playwright.ts",
    "scrape:bulk": "ts-node --project tsconfig.node.json scripts/scrape-high-volume.ts",
    "scrape:companies": "ts-node --project tsconfig.node.json scripts/scrape-companies.ts",
    "scrape:companies:bulk": "ts-node --project tsconfig.node.json scripts/scrape-companies-bulk.ts",
    "scrape:companies:optimized": "ts-node --project tsconfig.node.json scripts/scrape-companies-optimized.ts",
    "scrape:companies:safe": "ts-node --project tsconfig.node.json scripts/scrape-companies-safe.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.1",
    "playwright": "^1.55.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
